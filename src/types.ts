export type labeClass = {
    id: number,
    name: string,
    module: string
}

export type LabeStudent = {
    id:number,
    name:string,
    email:string,
    birth_date:string,
    class_id:number
}

export type LabeTeacher = {
    id: number,
    nome: string,
    email: string,
    data_nasc: string,
    turma_id: number
 }
