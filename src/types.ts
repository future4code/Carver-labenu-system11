export type labeClass = {
    id: string,
    name: string,
    module: string
}

export type Teacher = {
    id: string,
    nome: string,
    email: string,
    data_nasc: string,
    turma_id: string
 }